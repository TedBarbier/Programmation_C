#include <stdio.h>

int main(int argc, char * argv[]){
    int nombre;
    printf("donnez un nombre a mettre au carre :");
    scanf("%d", &nombre);
    printf("%d\n", nombre*nombre);
    return 0;
}